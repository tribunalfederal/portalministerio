rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGRAS PERMISSIVAS PARA BRASILÂNDIA RP
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
